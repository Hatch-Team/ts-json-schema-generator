{
  "$ref": "#/definitions/MyObject",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "definitions": {
    "Map_A": {
      "enum": ["a"],
      "type": "string"
    },
    "Map_B": {
      "enum": ["a"],
      "type": "string"
    },
    "Map_C": {
      "enum": ["a"],
      "type": "string"
    },
    "Map_D": {
      "enum": ["d"],
      "type": "string"
    },
    "Map_E": {
      "enum": ["d"],
      "type": "string"
    },
    "Map_F": {
      "enum": ["f"],
      "type": "string"
    },
    "MyObject": {
      "additionalProperties": false,
      "properties": {
        "a": {
          "$ref": "#/definitions/Map_A"
        },
        "b": {
          "$ref": "#/definitions/Map_B"
        },
        "c": {
          "$ref": "#/definitions/Map_C"
        },
        "d": {
          "$ref": "#/definitions/Map_D"
        },
        "e": {
          "$ref": "#/definitions/Map_E"
        },
        "f": {
          "$ref": "#/definitions/Map_F"
        }
      },
      "required": ["a", "b", "c", "d", "e", "f"],
      "type": "object"
    }
  }
}
